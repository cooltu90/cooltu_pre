package com.codingtu.cooltu.processor.worker;

/**************************************************
 *
 * 此处的顺序决定了写入顺序
 *
 **************************************************/
public enum ModelType {
    DEFAULT,
    code4Request,
    pass,
    actBase,
    layout,
    createAct,
    createFragment,
    manifest,
    start,
    apiService,
    sendParams, netBack, permission, fragmentBase, vh, mocks, adapter, net
}
